SELECT DISTINCT name FROM ratings JOIN movies ON ratings.movie_id = movies.id AND ratings.rating >= 9.0 JOIN directors ON directors.movie_id = movies.id JOIN people ON directors.person_id = people.id;